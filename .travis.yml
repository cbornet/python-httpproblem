dist: trusty
language: python

addons:
  sonarcloud:
    organization: "sonarcloud_organization_key"
    token:
      secure: 1234

install:
  - python3 --version
  - pip install tox

script:
  - tox
  - sonar-scanner
