dist: trusty

addons:
  sonarcloud:
    organization: "sonarcloud_organization_key"
    token:
      secure: 1234

install:
  - python --version
  - pip --version
  - pip install pip --upgrade
  - pip --version
  - pip install tox

script:
  - tox
  - sonar-scanner
