dist: trusty
language: python

addons:
  sonarcloud:
    organization: "cbornet-github"

install:
  - pyenv global system 3.6
  - pip install tox

script:
  - tox
  - cat coverage.xml
  - sonar-scanner --debug
